{
  "name": "peeler",
  "description": "parser for only bracket.",
  "version": "1.3.1",
  "author": "elzup",
  "repository": "elzup/peeler",
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "25.2.3",
    "@types/node": "12.12.3",
    "eslint": "7.2.0",
    "eslint-config-nzap": "2.0.0",
    "jest": "26.0.1",
    "prettier": "2.0.5",
    "ts-jest": "26.1.0",
    "ts-node": "8.10.2",
    "typescript": "3.9.3"
  },
  "engines": {
    "node": ">=8"
  },
  "files": [
    "dist"
  ],
  "keywords": [
    "brace",
    "bracket",
    "parser",
    "syntax"
  ],
  "license": "MIT",
  "lint-staged": {
    "scripts/**/*.ts": [
      "eslint --fix",
      "git add"
    ]
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build tsconfig.build.json",
    "deploy": "yarn build && yarn publish",
    "lint": "eslint --ext .ts src",
    "test": "yarn lint && jest"
  }
}
